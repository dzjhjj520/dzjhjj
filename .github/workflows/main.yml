name: AI Reply Bot (Fixed)

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  reply:
    runs-on: ubuntu-latest

    steps:
      - name: Install jq
        run: sudo apt-get install jq

      - name: AI Reply (DeepSeek)
        run: |
          RESPONSE=$(curl https://api.deepseek.com/v1/chat/completions \
            -H "Authorization: Bearer ${{ secrets.DEEPSEEK_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{
              "model": "deepseek-chat",
              "messages": [
                {
                  "role": "system",
                  "content": "你是一个专业的GitHub助手，回答简洁清晰。"
                },
                {
                  "role": "user",
                  "content": "${{ github.event.issue.title }}\n${{ github.event.issue.body }}"
                }
              ]
            }')

          echo "==== RAW RESPONSE ===="
          echo "$RESPONSE"
          echo "======================"

          COMMENT=$(echo "$RESPONSE" | jq -r '.choices[0].message.content // "AI无回复"')

          echo "==== COMMENT ===="
          echo "$COMMENT"
          echo "================="

          curl -X POST \
            -H "Authorization: Bearer ${{ github.token }}" \
            -H "Content-Type: application/json" \
            https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
            -d "{\"body\": \"$COMMENT\"}"