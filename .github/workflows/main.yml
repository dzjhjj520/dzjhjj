const title = context.payload.issue.title;

let reply = "默认回复";

if (title.includes("bug")) {
  reply = "检测到 Bug，我们会优先处理 🔧";
}

await github.rest.issues.createComment({
  ...context.issue(),
  body: reply
});
